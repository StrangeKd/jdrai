// PLACEHOLDER — Story 3.1 will implement this route
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/onboarding/welcome")({
  component: () => <div>Welcome — Coming soon (Story 3.1)</div>,
});
