// PLACEHOLDER — Story 4.x will implement the Hub
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/_authenticated/hub/")({
  component: () => <div>Hub — Coming soon (Story 4.x)</div>,
});
