export interface RaceDTO {
  id: string;
  name: string;
  description?: string;
  isDefault: boolean;
}

export interface CharacterClassDTO {
  id: string;
  name: string;
  description?: string;
  isDefault: boolean;
}
